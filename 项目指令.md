# 息壤杯 · 农资CRM 项目指令

## 项目简介
县域农资店AI农户客户运营系统，面向乡镇农资个体户的智能管理工具。

## 项目结构
- `deploy/` — 核心代码目录（所有开发都在这里）
  - `index.html` — 页面入口
  - `server.js` — HTTP服务器（天气/价格代理）
  - `app.js` — 主程序（页面渲染 + 启动）
  - `style.css` — 全部样式
  - `lib/` — 功能模块
    - `db.js` — 数据库 + 导航 + 初始化
    - `seed.js` — 种子数据
    - `ai.js` — AI 答疑引擎
    - `weather.js` — 天气模块
    - `market.js` — 市场价格模块
    - `notify.js` — 消息推送
    - `persist.js` — IndexedDB 持久化

## 开发规则
1. 新增功能请在 `lib/` 下建模块文件
2. `server.js` 只放 API 代理，不放业务逻辑
3. `app.js` 只放页面渲染函数（loadDashboard/loadFarmers 等）
4. 所��� SQL 操作通过 `sqlRun/sqlAll/sqlOne` 执行，自动持久化
5. 修改后执行 `node --check 文件名.js` 检查语法

## 运行方式
```bash
cd deploy
node server.js          # 启动服务器（端口3001）
npx localtunnel --port 3001  # 公网穿透（可选）
```

## 数据源
- 天气：和风天气 API（需配置 KEY）
- 价格：农业农村部 pfsc.agri.cn（免费，无需 KEY）
- 本地：IndexedDB 持久化

## API 代理
- `/api/qweather/weather` — 实时天气
- `/api/agri/prices` — 农产品价格
- `/api/weather` — 天气回退方案
