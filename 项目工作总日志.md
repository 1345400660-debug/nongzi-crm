# 农资CRM · 项目工作总日志

> 项目：县域农资店AI农户客户运营系统
> 赛事：2026"息壤杯"全国人工智能OPC创新大赛 — 惠民产品创新赛道
> 学校：南京邮电大学 · 电子信息工程
> 周期：2026-07-23 ~ 2026-07-30

---

## 一、项目概览

面向乡镇农资/兽药/农具个体户的AI驱动客户运营管理系统，四大核心功能：

| 功能 | 说明 |
|------|------|
| 🌱 种养周期档案 | AI标注关键生产节点，覆盖8种种植+8种养殖品类 |
| 📬 节点化精准触达 | 播种/防疫/收成前自动推送提醒 |
| 🤖 农技AI答疑 | 病虫害诊断、施肥方案、饲料配方、防疫免疫全套规则引擎 |
| 💰 账期温和提醒 | 按收成周期自动生成还款提醒，逾期自动检测 |

---

## 二、架构演进

### v1.0 — Node.js Express 全栈 (07-23 下午)
- 后端：Express + sql.js (SQLite WebAssembly) + better-sqlite3
- 前端：SPA 单页应用，5个页面
- 端口：3000
- 种子数据：10户农户 + 11条赊账 + 7条AI答疑 + 20种产品

### v2.0 — 全面优化 (07-23 17:00-17:30)
- AI引擎扩展：病虫害覆盖全部8种作物，养殖覆盖8种品类
- 修复12处XSS漏洞，空值保护，输入长度限制
- 性能：Dashboard 8次查询合并为1次，搜索300ms防抖，进度缓存
- UI：响应式布局、骨架屏、按钮防重复、确认对话框、Toast动画

### v3.0 — 纯前端架构 + 和风天气 (07-23 17:44-23:52)
- **架构转型**：Express后端 → 纯前端应用（sql.js WebAssembly在浏览器运行）
- **单文件时代**：`deploy/index.html` ~190KB 自包含单文件
- **和风天气集成**：实时天气 + 7日预报 + 6项生活指数
- **Open-Meteo 同源代理**：解决跨域问题，server.js 新增 `/api/weather` 代理
- **新增页面**：天气预报、行情价格、消息推送
- **农户扩展**：10户→25户，产品20种→30种
- **侧边栏**：收缩/展开功能，三种设备适配
- **部署**：CloudStudio 静态托管 (https://6d524f0ef2ba4dc18870e134b010f723.app.codebuddy.work)

### v4.0 — 模块化重构 + 实时数据 (07-24)
- **轻量重构**：190KB单文件 → 8个模块文件
  - `style.css` — 样式独立
  - `lib/db.js` — SQL封装 + 导航 + 初始化
  - `lib/seed.js` — 种子数据
  - `lib/ai.js` — AI答疑引擎
  - `lib/weather.js` — 天气模块
  - `lib/market.js` — 市场价格模块
  - `lib/notify.js` — 消息推送模拟
  - `app.js` — 页面渲染 + 启动入口
- **农业农村部实时价格**：对接 `pfsc.agri.cn` 官方API，获取全国批发价格指数
- **天气离线兜底**：API不可用时自动切换为本地估算（按纬度+季节）
- **价格离线兜底**：API失败时自动切换至模拟区域价格
- **Bug修复**：
  - `seedData()` 每次刷新重复插入数据 → 加判重检查
  - app.js 残留337行重复代码（覆盖 lib/ 模块） → 清除
  - 价格API请求无超时 → 添加10s超时
- **IndexedDB 数据持久化**：
  - 新增 `lib/persist.js` — IndexedDB 读写封装
  - 每次数据变更后自动保存（INSERT/UPDATE/DELETE触发）
  - 页面刷新后自动恢复，数据不再丢失

### v4.1 — 公网穿透 (07-30)
- **localtunnel** 公网穿透，手机随时随地访问
- 地址：https://smooth-rabbit-84.loca.lt
- 天气和价格均为真实数据（需本地 `server.js` 运行）
- 免费版每2小时需重连

---

## 三、技术栈

| 层级 | 技术 |
|------|------|
| 前端 | 原生 JavaScript (ES5兼容) + CSS3 设计系统 |
| 数据库 | SQL.js (SQLite WebAssembly) |
| 后端代理 | Node.js HTTP Server (无框架) |
| 天气数据 | 和风天气 API (QWeather) |
| 价格数据 | 农业农村部 pfsc.agri.cn API |
| 持久化 | IndexedDB |
| 部署 | CloudStudio / localtunnel |
| 数据模拟 | 基于政府报告的真实基准价 + 区域系数模型 |

---

## 四、项目目录结构

```
nongzi-crm/
├── deploy/                     # 部署目录（核心）
│   ├── index.html              # 入口（HTML骨架）
│   ├── style.css               # 全部样式
│   ├── app.js                  # 主程序（页面渲染 + 启动）
│   ├── server.js               # HTTP服务器（天气/价格代理）
│   ├── sql-wasm.js             # SQL.js WebAssembly 运行时
│   ├── sql-wasm.wasm           # SQL.js WebAssembly 二进制
│   ├── cloudflared.exe         # 隧道工具（可选）
│   ├── lib/
│   │   ├── db.js               # 工具函数 + SQL封装 + 导航 + initApp
│   │   ├── seed.js             # 种子数据（25户+产品+赊账+AI记录）
│   │   ├── ai.js               # AI答疑规则引擎
│   │   ├── weather.js          # 天气模块（区域选择 + API + 离线兜底）
│   │   ├── market.js           # 市场价格模块（实时API + 区域模拟）
│   │   ├── notify.js           # 消息推送模拟
│   │   └── persist.js          # IndexedDB 持久化
│   └── pages/                  # (预留)
├── public/
│   └── index.html              # 旧版前端（v2.0 保留）
├── server.js                   # 旧版Express后端（v1.0 保留）
├── seed.js                     # 旧版种子数据脚本
├── weather_market_new.js       # 旧版天气市场模块（v3.0 保留）
└── nongzi.db                   # SQLite数据库文件
```

---

## 五、API 代理端点

| 端点 | 说明 | 外部数据源 |
|------|------|-----------|
| `/api/qweather/weather` | 聚合天气(实时+7日预报+生活指数) | 和风天气 API |
| `/api/weather` | 天气回退方案 | Open-Meteo API |
| `/api/agri/prices` | 农产品批发价格日报 | 农业农村部 pfsc.agri.cn |

---

## 六、运行方式

```bash
cd deploy
node server.js         # 启动服务器（端口3001）
# 另开终端
npx localtunnel --port 3001   # 公网穿透（可选）
```

---

## 七、待办 / 可优化方向

- [ ] **轻量云服务器部署**：买一台云服务器，24小时在线，不再依赖本地电脑
- [ ] **更多数据源**：接入惠农网API，获取更细粒度的单品价格
- [ ] **数据报表可视化**：图表展示农户增长、赊账趋势等
- [ ] **Excel/PDF导出**：农户档案、账期报表导出
- [ ] **农户端小程序**：农户手机端查看自己的种养周期和账期
- [ ] **真实消息推送**：对接短信API/微信模板消息

---

> 生成时间：2026-07-30 14:20
> 最后编辑：2026-07-30
